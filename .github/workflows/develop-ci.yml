name: Develop CI

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop
  workflow_dispatch:

env:
  APP_NAME: man-ng
    
jobs:
  # unit-tests:
  #   runs-on: ubuntu-latest
  #   name: Unit tests
    
  #   steps:
  #     - uses: actions/checkout@v2

  #     - uses: actions/setup-node@v2
  #       with:
  #         node-version: '16'
  #         cache: 'npm'
  #         cache-dependency-path: ./package-lock.json

  #     - name: Install dependencies
  #       run: npm install

  #     - name: Run unit tests
  #       run: npm run test -- --no-watch --browsers=ChromeHeadlessCI --code-coverage

  #     - uses: actions/upload-artifact@v3
  #       with:
  #         name: coverage-report
  #         path: ./coverage

  # code-ql:
  #   name: CodeQL
  #   runs-on: ubuntu-latest
  #   permissions:
  #     actions: read
  #     contents: read
  #     security-events: write

  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       language: [ 'javascript' ]

  #   steps:
  #   - name: Checkout repository
  #     uses: actions/checkout@v2

  #   - name: Initialize CodeQL
  #     uses: github/codeql-action/init@v1
  #     with:
  #       languages: ${{ matrix.language }}

  #   - name: Autobuild
  #     uses: github/codeql-action/autobuild@v1

  #   - name: Perform CodeQL Analysis
  #     uses: github/codeql-action/analyze@v1

  deploy:
    uses: kubeopsskills-enterprise/ais-share-pipeline/.github/workflows/angular-develop-cd.yml@latest
    with:
      APP_NAME: man-ng
      ARTIFACT_NAME: artifact-man-ng-449fde6dc6941392193af7c551d5ed04c805f30f.zip